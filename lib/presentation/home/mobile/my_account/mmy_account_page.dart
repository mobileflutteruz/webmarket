import 'package:flutter/material.dart';

class MMyAccountPage extends StatelessWidget {
  const MMyAccountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center( 
        child: Text('My Account')
      ),
    );
  }
}